document.addEventListener("DOMContentLoaded", function () {
  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "TOWER_ACCESS_TOKEN (Optional)") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });
});
document.addEventListener("DOMContentLoaded", function () {
  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Resume") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });
});

document.addEventListener("DOMContentLoaded", function () {
  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Input/output options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Main options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "FASTQ Preprocessing") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Preprocessing") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Variant Calling") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Annotation") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "General reference genome options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Reference genome options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });
});
