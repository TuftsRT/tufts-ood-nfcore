document.addEventListener("DOMContentLoaded", function () {
  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "TOWER_ACCESS_TOKEN (Optional)") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });
});
document.addEventListener("DOMContentLoaded", function () {
  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Resume") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });
});

document.addEventListener("DOMContentLoaded", function () {
  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Input/output options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Wfmash Options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Seqwish Options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Smoothxg options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Vg Deconstruct Options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Community") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });
});
