document.addEventListener("DOMContentLoaded", function () {
  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "TOWER_ACCESS_TOKEN (Optional)") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });
});
document.addEventListener("DOMContentLoaded", function () {
  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Resume") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });
});

document.addEventListener("DOMContentLoaded", function () {
  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Input/output options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Abundance values") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Observations (e.g. samples) options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Features options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Affy input options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Proteus input options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Filtering") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Exploratory analysis") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Differential analysis") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "DESeq2 specific options (RNA-seq only)") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Limma specific options (microarray only)") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "GSEA") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "gprofiler2") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Shiny app settings") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Options related to gene set analysis") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Reporting options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });

  document.querySelectorAll("label").forEach((label) => {
    if (label.textContent.trim() === "Reference genome options") {
      label.style.color = "green";
      label.style.fontWeight = "bold";
    }
  });
});
